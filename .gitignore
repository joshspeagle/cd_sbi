# LaTeX build artifacts
*.aux
*.bbl
*.bcf
*.blg
*.fdb_latexmk
*.fls
*.lof
*.log
*.lot
*.out
*.pdf
*.run.xml
*.synctex.gz
*.toc
*.nav
*.snm
*.vrb

# Editor / OS
.DS_Store
Thumbs.db
*.swp
*~

# Local-only working notes / session handoffs (kept on disk, never committed)
SESSION_HANDOFF.txt

# Python (for any future SBI experiments)
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# Local config
.env
.env.local

# CD-SBI experiment outputs
outputs/

# sbi package run-time logs (per-train tensorboard / metrics dirs)
sbi-logs/

# Python build artifacts
.pytest_cache/
.coverage
.claude/
*.tmp.*

# Visualization suite outputs ARE tracked (override the global *.pdf rule above).
# Use the ** form so nested figure subdirs (e.g. figures/section_8/e1.pdf) are
# also un-ignored, not just files directly in figures/. PNGs are not globally
# ignored, but negate them too for clarity / future-proofing.
!figures/**/*.pdf
!figures/**/*.png
!figures/*.pdf
!figures/*.png
