name: adam_cosine
optimizer: adam
lr: 3.0e-3
batch_size: 512
n_steps: 4000
n_epochs: 100
n_train: 10000
fresh_batch: true

weight_decay: 0.0
betas: [0.9, 0.999]

lr_schedule: cosine
lr_min_ratio: 0.01

batching: random_replacement
grad_clip_norm: 5.0
